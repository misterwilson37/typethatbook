ROUND 114 (Carriage) — 2026-09-10 — UPLOAD ME FIRST

Expected build stamps after deploy:
    arcade.html   badge reads   arcade v3.14.0
    learn2.html   footer reads  0.5.0-staging   (learn2.html itself is UNCHANGED)

Read those before reporting any bug against this round. If the arcade badge still
says 3.13.0, the files are not applied and nothing below is being tested.

Suite as delivered: 1 failing of 96 harnesses.
That one failure is guest-merge-test.mjs — pre-existing, untouched, undiagnosed,
logged as ROADMAP 114a. Any SECOND failure is something in here.

════════════════════════════════════════════════════════════════════════════════
1. REPO ROOT  (same folder as index.html)
════════════════════════════════════════════════════════════════════════════════
  arcade.html        v3.14.0          Deadline's console in arcade mode; Shatter
                                      in the dropdown; empty threat box collapsed
  game-names.js      v1.2.0           Shatter un-flagged; panelOptionsFor()
  game-escape.js     v2.3.0           fifth creature; kaiju peek staging
  game-draw.js       v1.13.0          peekStaging(); derived queue row count
  game-slot.js       v1.0.0    NEW    where Deadline goes in a lesson
  run-picker.js      v1.0.0    NEW    which run a student may start
  learn2.js          v0.5.0-staging   run picker fixed; Deadline in 46/47 lessons
  README.md                           now the ONLY readme
  HANDOFF.md                          now the ONLY handoff
  ROADMAP.md
  CHANGELOG.md

⚠️ learn2.html is NOT in this zip and needs no change. Its two new modules are
imported by learn2.js, and both are plain files at the repo root.

════════════════════════════════════════════════════════════════════════════════
2. tests/
════════════════════════════════════════════════════════════════════════════════
  tests/game-slot-test.mjs        NEW  83 assertions, real 47-lesson corpus
  tests/run-picker-test.mjs       NEW  26 assertions, real record shape
  tests/escape-peek-test.mjs      NEW  21 assertions, real canvas transform
  tests/arcade-panels-test.mjs    v1.4.0
  tests/arcade-versions-test.mjs  v1.1.0
  tests/game-assumptions-test.mjs v1.1.0
  tests/undefined-calls-test.mjs  v1.4.0
  tests/run-all-tests.mjs         v1.29.0   registers all three new harnesses

════════════════════════════════════════════════════════════════════════════════
3. ⚠️  DELETE THESE FIVE FROM THE REPO ROOT
════════════════════════════════════════════════════════════════════════════════
  HANDOFF-games.md
  HANDOFF-learn2.md
  INTEGRATION.md
  NEXT-STEPS.md
  README-games.md

All five are now sections inside HANDOFF.md (§§10–14) or an appendix inside
README.md (Appendix A), appended VERBATIM — nothing summarised or edited away.
Their rows in HANDOFF §9's document map are struck through with a note saying
where the content went.

⚠️ THE DELETIONS ARE THE POINT, NOT HOUSEKEEPING. Leaving them means two copies
of every warning in them, which is the Rule 9 failure this project keeps finding
— and one readme / one handoff was the request.
`tests/docs-vs-repo-test.mjs` goes RED if a .md sits in the repo that HANDOFF §9
does not list, so a missed deletion is caught rather than silent.

Root .md files after this round, and that is the whole list:
  README.md   HANDOFF.md   ROADMAP.md   CHANGELOG.md

════════════════════════════════════════════════════════════════════════════════
4. WHAT TO TRY ON THE LIVE SITE
════════════════════════════════════════════════════════════════════════════════
arcade.html
  • Deadline with WORDS set to "everything — the whole word pool" now has a
    radar, a city-status board and a console. That was the reported bug.
  • Shatter appears in the GAME dropdown for the first time.
  • Escape Key's left panel shows five creatures, with no empty box beneath.
  • A kaiju entering from the RIGHT leans INTO the board.
  • The page title tracks the game you picked instead of always saying DEADLINE.
  • No "— just for fun" suffix on any game.

learn2.html
  • Open a lesson you left part-way. It should open ON the run you reached, with
    every earlier run one click away in the intro panel, and the next one
    unlocked. Records written on learn.html are now readable here.
  • A Unit 1, 2 or 5 lesson should now END IN DEADLINE, replacing the final
    drill run — paced by that lesson's own WPM, and still graded on ACCURACY
    ONLY, exactly as the drill was.
  • A passage lesson still types 1/4 through 4/4 and then appends the game, with
    chunk 1 as the pass bar and survival after it for bragging rights.

⚠️ THE THING TO WATCH FOR AND TELL ME ABOUT: whether a Unit 1 or 2 lesson now
feels harder to PASS than it did. It should not — the speed number only decides
how fast words fall, never whether the child passed. If a student who used to
pass now fails, that is Option 2 leaking in and it is a stop-everything bug.

════════════════════════════════════════════════════════════════════════════════
5. NOT IN THIS ZIP, ON PURPOSE
════════════════════════════════════════════════════════════════════════════════
  package.json    — npm install rewrites it (bumps dependency ranges and
                    un-escapes the unicode in the "//" comment arrays). Held
                    byte-identical to your upload; nothing to apply.
  game-sprites.js — UNCHANGED. Its screen-angle contract is now pinned by
                    escape-peek-test.mjs Part C, but not a byte moved.
  learn.html / learn.js — UNCHANGED. The production page still has no run picker
                    and no game slot; learn2 is where both live until you decide
                    to promote it (HANDOFF §11).

════════════════════════════════════════════════════════════════════════════════
6. FOR THE NEXT CLAUDE INSTANCE
════════════════════════════════════════════════════════════════════════════════
⚠️ RUN `npm install` BEFORE BELIEVING A FAILURE COUNT. A fresh container has no
node_modules and nine harnesses die on a missing package, looking exactly like
nine defects.

⚠️ WRITE FILES VIA A TEMP FILE AND os.replace(). This round emptied
run-all-tests.mjs by opening it with mode 'w' (which truncates) and then throwing
on an encoding error. The suite then reported NOTHING and exited 0 — an empty
module is valid, so every check passed against a file with no tests in it.
